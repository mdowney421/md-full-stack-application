<!DOCTYPE html>
<html lang="en">
<%- include ('./partials/head.ejs') %>
<body>
    <%- include ('./partials/nav.ejs') %>
    <div class="jumbotron jumbotron-fluid">
        <div class="container">
            <h1 class="display-4">Your workout from <%=workout.date.toLocaleDateString('en-us', { weekday:"long", year:"numeric", month:"short", day:"numeric"})%></h1>
        </div>
    </div>
    <div class="row my-5">
        <div class="col-8 mx-auto">
            <form action="/workouts/<%=workout.id%>?_method=PUT" method="POST">
                <div class="form-group">
                    <label for="dateInput">Date</label>
                    <input name="date" class="form-control" id="dateInput" value="<%=workout.date.toLocaleDateString('en-us', {year:"numeric", month:"numeric", day:"numeric"})%>">
                </div>
                <div class="form-group">
                    <label for="workoutTypeInput">What type of workout did you do?</label>
                    <select name="type" class="form-control" id="workoutTypeInput" value="<%=workout.type%>">
                        <option>Running</option>
                        <option>Cycling</option>
                        <option>Strength Training</option>
                        <option>Swimming</option>
                        <option>HIIT</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="durationInput">How long did you work out (in minutes)?</label>
                    <input name="duration" class="form-control" id="durationInput" value="<%=workout.duration%>">
                </div>
                <div class="form-group">
                    <label for="caloriesBurnedInput">Calories burned:</label>
                    <input name="caloriesBurned" class="form-control" id="typeInput" value="<%=workout.caloriesBurned%>">
                </div>
                <div class="form-group">
                    <label for="notesInput">Notes</label>
                    <textarea class="form-control" id="notesInput" rows="3" value="<%=workout.notes%>"></textarea>
                </div>
                <a href="/workouts/<%=workout.id%>">
                    <button type="button" class="btn">Cancel Changes</button>
                </a>
                <input type="submit" value="Save Changes" class="btn btn-success"/>
            </form>
        </div>
    </div>
    <%- include ('./partials/footer.ejs') %>
</body>
</html>